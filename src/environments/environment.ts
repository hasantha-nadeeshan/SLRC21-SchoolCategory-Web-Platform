// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCOJqZvzBW_vKeSp3m0PgNDkYcOsHiw8ic",
    authDomain: "slrc-school.firebaseapp.com",
    projectId: "slrc-school",
    storageBucket: "slrc-school.appspot.com",
    messagingSenderId: "211600032105",
    appId: "1:211600032105:web:4c71712a98598d6759bdf8",
    measurementId: "G-CFXFEHZJCW"
}
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
